apiVersion: v1
kind: Pod
metadata:
  name: result-app-service
  labels:
    name: result-app-service
    app: demo-voting-app
spec:
  type: LoadBalancer
  ports:
  - ports: 80
    targetPort: 80
  selector:
    name: result-app-pod
    app: demo-voting-app
